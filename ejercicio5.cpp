#include <cassert>
#include <string>
#include <iostream>
#include <limits>

using namespace std;



int main()
{
    cin >> m;

    for (int i = 0; i < m; i++)
    {
        cin >> idMision;
        cin >> nombreMision;

        
    }

    cin >> c;
    cin >> o;

    for (int i = 0; i < c; i++)
    {
        cin >> idCiudad;
        cin >> nombreCiudad;



    }

    cin >> e;

    for (int i = 0; i < e; i++)
    {
        cin >> idCiudadOrigen;
        cin >> idCiudadDestino;
        cin >> tiempoDesplazamiento;


    }
    return 0;
}